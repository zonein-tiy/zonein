json.(@workout, :name, :description, :time, :image_url, :video_url, :running, :weightlifting, :swimming, :circuit_training, :cycling, :steps)

# json.creator do
#   json.name @workout.creator.name
# end
